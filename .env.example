# ── Server ──────────────────────────────────────────────────────────
# Agnes API key & base URL are entered in the frontend (per-user),
# NOT stored on the server. This file only configures the local server.
HOST=0.0.0.0
PORT=8000
